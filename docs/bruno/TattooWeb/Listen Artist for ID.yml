info:
  name: Listen Artist for ID
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:5266/api/artists/{{Id_Artist}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
