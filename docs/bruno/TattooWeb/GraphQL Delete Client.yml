info:
  name: GraphQL Delete Client
  type: graphql
  seq: 16

graphql:
  method: POST
  url: http://localhost:5266/graphql
  body:
    query: |-
      mutation {
        deleteClient(id: "cole-o-id-aqui")
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
