info:
  name: Listen Artist
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:5266/api/artists
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
